<UserControl
    x:Class="Utilities.Landscape"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Utilities"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="320">
    
    <UserControl.Resources>

    </UserControl.Resources>

    <Grid x:Name="LayoutRoot">
        <GridView x:Name="LandscapeContent" HorizontalAlignment="Stretch" SelectionMode="Single" 
                   Visibility="Visible" SelectionChanged="LandscapeContent_SelectionChanged" 
                  >

        </GridView>

        <ListView x:Name="LandscapeContentSnapped" VerticalAlignment="Stretch"  SelectionMode="Single"
                    Visibility="Collapsed" SelectionChanged="LandscapeContent_SelectionChanged" />
    </Grid>

</UserControl>
